# Copy this file to .env.local and fill in the values. .env.local is gitignored.

# ---- MongoDB ----
# Server-only. NEVER prefix with NEXT_PUBLIC_.
MONGODB_URI=
MONGODB_DB_NAME=night_canteen

# ---- Staff accounts ----
# The first boot creates this admin if no admin exists yet.
ADMIN_USERNAME=
ADMIN_PASSWORD=
# Staff sign in with a username; the app stores it internally as
# <username>@<domain>. This address never receives mail and the domain does not
# need to exist. Change it only before creating accounts — existing logins are
# keyed on the old value.
ADMIN_EMAIL_DOMAIN=nightcanteen.local

# ---- Weather accents (optional) ----
# The customer menu tints itself by time of day + live weather over campus.
# Defaults to Karjat / Vijaybhoomi. No API key needed (Open-Meteo).
# CAMPUS_LAT=18.9107
# CAMPUS_LON=73.3233

# ---- Cashfree Payments ----
# Dashboard → Developers → API Keys. Start in Sandbox.
CASHFREE_APP_ID=
CASHFREE_SECRET_KEY=
# "sandbox" or "production". Defaults to sandbox, so a missing value can never
# silently start charging real cards.
CASHFREE_ENV=sandbox
#
# NOTE: there is no separate webhook secret. Cashfree signs webhooks with
# CASHFREE_SECRET_KEY above, so setting that one value covers both.
